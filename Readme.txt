1.1 Для установки выполните SQL запрос к базе данных MySQL:

CREATE TABLE users (
    id INT(11) NOT NULL AUTO_INCREMENT,
    name TINYTEXT NOT NULL,
    level INT(11) NOT NULL,
    age INT(11) NOT NULL,
    
    PRIMARY KEY(id)
    );

INSERT INTO users(id, name, level, age) 
VALUES (null, 'Test', 0, 10);

INSERT INTO users(id, name, level, age) 
VALUES (null, 'Beta', 1, 20);

INSERT INTO users(id, name, level, age) 
VALUES (null, 'Alpha', 2, 30);

1.2 Пропишите данные от базы данных в файле "applicaton/core/DB.php" (там все прокомментировано)
1.3 Установка выполнена. Перейдите по адрессу: http://ваш_сайт/

Так, исправления(возможно не в лучшую сторону):
     * создан абстрактный класс DB.php в котором, собственно исходя из названия, "случаются" операции с БазойДанных...
	- Возможно, Вы, не так просили реализовать его... Если что - пишите, я исправлюсь.
     * создан класс Request.php через объект которого нужно обращаться к $_REQUEST используя метод: getValue(string $string);
     * получение данных и их обработка теперь происходит в контроллере, не как раньше.(получение данных через $_SERVER[...], $_REQUEST[...]
	 раньше происходило в моделе)
     * еще что-то...

Писал код исходя из своего опыта(конечно, маленького опыта, но попутно ознакомился со статьями в интернете), возможно, реализовал все не так как Вы ожидали...

Удачного, Вам, дня!